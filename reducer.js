const init = {
    cars = ['BMW']
}

export default function reducer(state, action, args) {
    switch (action) {
        default: 
            case 'CREATE'

                break
            return state 
    }
}